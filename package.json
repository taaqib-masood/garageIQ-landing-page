{
  "name": "garageiq",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "echo 'Static landing page deployment. Bypassing Next.js workspace build.'",
    "lint": "npm run lint --workspaces"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
