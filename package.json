{
  "name": "garageiq-landing-page",
  "private": true,
  "description": "Static marketing site for GarageIQ. Everything shipped lives in public/; vercel.json copies it to the build output. No build step and no dependencies.",
  "scripts": {
    "dev": "python3 -m http.server 8000 --directory public"
  }
}
